# import logging


class Logger:
    # def __init__(self):
        # logging.getLogger().setLevel(logging.INFO)
        # self.loggerHandle = logging

    def info(self, msg, *args, **kwargs):
        print(msg)
        #self.loggerHandle.info(msg, *args, **kwargs)
